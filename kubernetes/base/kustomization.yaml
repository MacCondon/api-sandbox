apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Namespace is set by each overlay (dev, staging, prod)

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/managed-by: argocd
